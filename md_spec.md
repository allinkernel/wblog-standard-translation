# Markdown 规范文档翻译规范（md_spec.md）

> **版本**：v1.0（2026-08-26，从 spec.md v2.0 拆出，聚焦"markdown 中间产物"场景）
> **适用**：① 源文件本身是 markdown（如 RFC markdown 版、GitHub 标准仓库、mdbook 文档）；② 源 HTML 需要转 markdown 作为中间产物（**仅限用户明确要求**，见 2.2 警告）；③ 产物（直译/导读）以 markdown 形式交付、经 pandoc 编译为 HTML 后嵌入博客。
> **不适用**：源与产物都是 HTML 的场景 → 见 `html_spec.md`（DOM 抽取→纯文本翻译→回填，格式保真度最高）。
> **配套**：`spec.md`（总入口与通用机制：三兄弟定义、触发审批、递归、成本、md5 双基线闭环、导读两阶段、知识卡片）。
> **执行区隔**：标有 **[Agent 执行]** 或"必须/禁止/一律/上限/铁律"的条目是硬性约束；其余为背景解释。

---

## 1. 总体流程

```
源获取（markdown 原样 / HTML 转 markdown，见 2）
        │
        ▼
md5 基线 + 分段投递（通用机制见 spec.md 3.4）
        │
        ▼
逐段直译（结构镜像 + 实体转义 + raw HTML 表格，见 3/4/5）
        │
        ▼
知识卡片 → knowledge-index → 两阶段导读（通用机制见 spec.md 4.5）
        │
        ▼
pandoc 渲染验证（见 7）
```

**核心差异（与 html_spec 的本质区别）**：本场景的产物是 **markdown 文本**，结构靠 markdown 语法表达、表格靠 raw HTML 内嵌、特殊字符靠实体转义防 pandoc 误解析——**markdown 的表达力是上限，超出表达力的结构（复杂表格、无表头表格、缩进层级）必须用 raw HTML 或降级手段补齐**。

---

## 2. 源获取

### 2.1 源本身就是 markdown

- 直接使用源文件，`md5sum 源文件 > 源文件.md5` 记录基线（通用机制见 spec.md 3.4）。
- **优先用官方/权威仓库版本**（如 RFC Editor、标准组织的 GitHub 仓库），不得凭记忆重建内容。

### 2.2 HTML 转 markdown（仅限用户明确要求，默认禁止）

> **警告（血的教训）**：pandoc 把 HTML 转 markdown 会丢失 `<dl compact>` 结构（Term 顶格 + dd 缩进）、丢图片引用（`WARNING: Could not fetch resource`）、破坏复杂表格。**除非用户明确要求 markdown 中间产物，否则一律走 `html_spec.md`（HTML 原样保存 + DOM 回填），禁止 pandoc 强转。**

若用户明确要求转换：

1. 转换前先下载官方 HTML 原样存档（`index.html` + md5 基线），转换只是衍生中间产物，**官方 HTML 才是权威底本**；
2. `pandoc -f html -t gfm index.html -o index.md`；
3. 转换后**必须核对**：`<dl>`/`<blockquote>`/`<pre>`/`<table>` 数量与源一致，图片引用无 `Could not fetch resource`（图片改 `.pic/` 相对路径）；
4. 转换后 md 中残留的原始 HTML `<table>` 保留（pandoc 直通），按第 5 节处理。

---

## 3. 结构镜像（markdown 表达）

"逐字逐句"三层次（语义零丢失 + 中文自然语序 + 文档结构镜像）的定义与翻译"一行文本"六步操作见 **spec.md 3.1 / 3.2**（通用）。本节只规定**镜像的 markdown 表达**：

| 原文结构 | 译文结构（markdown） |
| --- | --- |
| `<h2>/<h3>/<h4>/<h5>` 标题 | `##` / `###` / `####` / `#####`，标题文本 `中文 (English)` |
| `<ol>` 有序列表 | `1.` `2.` `3.`，**编号必须与原文一致**（原文跳号就跳号，不得重排） |
| `<ul>` 无序列表 | `-` |
| `<dl compact>` 定义列表 | pandoc 定义列表：Term 顶格 + 空行 + `:   ` 缩进内容（同一 dd 多段用 4 空格续行；dd 内嵌套 ul 用 4 空格缩进的 `- `；嵌套 pre 用 4 空格缩进 fenced 代码块） |
| `<blockquote>` | `> ` |
| `<pre>` 代码块 | fenced 代码块 ```` ``` ````（shell/程序代码**原样保留不译**；fenced 检测要 `ln.lstrip(': \t').startswith('```')`，因为定义列表 `:   ` 后的 fenced 开始行带冒号前缀） |
| `<table>` | **raw HTML 表格**（见第 5 节），不用 markdown 表格 |
| `$` / 反引号顶格标记 | **顶格**（行首无缩进），其描述段落**缩进**（`<dl compact>` 的典型形态） |
| `[Option Start]/[Option End]` 图标 | `[译者注：原文此处有可折叠 UI 标记（UP 图标与 [Option Start] 图片）...]` |

**[Agent 执行] 铁律**：
- 有序列表编号必须保留原文数字（1. 2. 3.），不得转成 `-` 或重排；
- 缩进层级必须镜像（`$`/反引号顶格、描述缩进）；
- 纯排版（项目符号字符、行宽、字体）**不复制**——用户明确"排版完全不需要管，结构层级必须保留"；
- 交叉引用链接 → `[中文标题](#原文锚点)` 或 `[中文标题](../path)`，**锚点 id 与原文一致**；
- 工具链接（如 `[*sh*](../utilities/sh.html)`）保留。

---

## 4. 实体转义（防 pandoc 误解析，本场景最关键的坑）

### 4.1 转义表

在 markdown 文本节点中，以下字符需转义为 HTML 实体，防止 pandoc 的 smart 转换和 markdown 解析破坏内容：

```
&   →  &amp;      <   →  &lt;       >   →  &gt;
"   →  &#34;      '   →  &#39;      $   →  &#36;
`   →  &#96;      *   →  &#42;      _   →  &#95;
[   →  &#91;      ]   →  &#93;      \   →  &#92;
--- →  &#45;&#45;&#45;   -- →  &#45;&#45;   ... →  &#46;&#46;&#46;
```

- 效果：`&#36;` 显示 `$` 不会触发 TeX math；`&#45;&#45;&#45;` 保持 `---` 字面不会被 smart 转成 em-dash；`&#34;` 显示 `"` 不会变弯引号。
- **[Agent 执行] 致命教训**：HTMLParser 解码后 `<`/`>` 若不重新编码为 `&lt;`/`&gt;` 会**直接丢失**（344 个 `&lt;` 全没了的教训：`exec 3&lt; readfile` 变 `exec 3 readfile`）。转义函数必须把 `<`/`>` 重新编码！
- **代码块内不需要转义**（pandoc 不解析代码块内容）；若代码块内误留 `\$`，恢复为 `$`（`\$` 会显示反斜杠）。

### 4.2 裸 `$` 转义边界（避免过度杀伤）

- Shell 变量/参数记号（`$var`、`${var}`、`$IFS`、`$1`、`"$@"`）**优先放入行内代码块**（`` `$var` ``）天然豁免转义，保持源码可读、用户可复制；
- 仅当 `$` 可能被 pandoc 误判为 TeX 数学（**成对出现**，如 `$a=b$`）或作为普通文本 `$` 时，才转义为 `&#36;`；
- **中文文本中的成对裸 `$` 必须处理**（如 `$1、$2 和 $3` 中 `$` 成对会触发 math 报错 `Could not convert TeX math`）：放代码块或转义。

---

## 5. 表格：raw HTML，逐格一致（最重要）

**markdown 表格是缺陷**（无 colspan/rowspan、列宽不可控、单元格内多行受限、必须有表头行），无法复刻官方表格。**所有表格必须用官方同款 raw HTML**，pandoc 对 markdown 中的 raw HTML 表格直通保留。

### 5.1 步骤

1. 从官方 `index.html` 提取对应 `<table>...</table>` 原文。
2. **保留官方结构**：`border`、`cellpadding`、`colspan`、`rowspan`、`align`、单元格内 `<br>` 多行、嵌套 `<p>` 全部保留。
3. 替换单元格文本为中文（代码/符号/函数名原样）。
4. 表格内文本**实体转义**（防 pandoc 对 raw HTML 内部做 markdown 解析），但**标签本身不转义**。
5. 单元格内 markdown 行内语法转 HTML：**先 esc_html（步骤 4）后**，行内转换要匹配**实体形式**（反引号已转成 `&#96;`、星号已转成 `&#42;`），或用占位符保护标签（见 5.3）。
6. 官方"1 行多列、每列 `<br>` 多项"的紧凑表（保留字表、awk 关键字表）→ 照抄官方布局，**不要**拆成多行 markdown 表。
7. 官方无表头的表格（如进程属性 2 列）→ raw HTML 无表头实现（markdown 表格必须有表头行，做不到）。
8. **列数必须与官方一致**：官方 14 列（类型字母 + 类型名 + 11 动作列 + 函数列）就不能译成 13 列——渲染错位、最后一列全错。用 python 校验每行 `|` 数（raw HTML 则数 `<td>`/`<th>`）。

### 5.2 致命顺序 bug 的闭环解法（占位符三步法，照抄）

1. 【保护】用正则提取单元格内所有原生 HTML 标签（`<code>`、`<i>`、`<a href=...>` 等），替换为唯一占位符（`__TAG_PLACEHOLDER_0__`…）；
2. 【转义】对剩余纯文本执行实体转义（裸 `$`、`*`、`<`、`>` 等）；
3. 【还原】将占位符原样还原为 HTML 标签。

> 反例（禁止）：`esc_html(inline_md(text))` 是错的——inline_md 生成 `<code>` 等标签后，esc_html 会把 `<` 转义成 `&lt;`，渲染成字面 `<code>` 文本。

### 5.3 推荐：表格处理交给 Python 脚本而非 LLM 手工转义

LLM 手工转义大表格出错率极高。脚本 `table_worker.py`：

- 解析官方 `index.html` 的 `<table>` → 保护标签 → 提取纯文本给 LLM 翻译；
- **[Agent 执行] 表格语义保护**：抽离的纯文本投递 LLM 时必须附带语义上下文（所属表题、行头、列头），JSON 或注释块形式，如 `{"context": "表 2-1 退出状态：列=Exit Status", "text": "0"}`——否则"0"可能被误译为"零"而非"退出状态 0"；
- LLM 返回译文纯文本 → 脚本自动拼回 HTML 标签（先 HTML 转义再回填，节点数/顺序断言）。

该方案表格准确率近 100%，且省 token（LLM 只处理纯文本）。

### 5.4 验证

- pandoc 渲染后检查 `&lt;code&gt;`/`&lt;i&gt;` 等字面标签残留为 0；
- 表格行数、列数、colspan 与官方逐一比对（脚本解析 `<tr>`/`<td>` 对比）；
- 官方 1 行多列紧凑表 vs markdown 多行表需**转置**时（如官方内在实用程序 1 行 6 列），转置后必须逐格核对，防止错位。

---

## 6. 标题铁律（直译与导读通用，用户明确要求）

- 可以有自己的 **0. 章节**（如 `## 0. 导读总览`、`### 0.1 ...`）——0 章节不参与原文编号。
- **从 1. 开始的标题必须与原文编号一一对应**：`## 1. Introduction`、`### 1.1 ...`、`#### 1.1.1 ...`、`##### 1.1.1.1 ...`，格式 `中文 (English)`，与直译一致。
- **禁止**双重编号（如 `### 1.2 1.1.1 System Interfaces`）和"导读第 X 节"式标题（如 `### 1.4 第 1.2 节：...导读`）。
- 导读自己的细分（"为什么存在"/"核心机制"/"速查表"等）用**无编号粗体** `**...**`，不参与编号体系。
- 工具速查（如 2.16–2.30）每个工具独立标题 `### 2.16 break`，顺序与原文一致，**不得合并多个工具到一个标题**。
- **原文无子编号时不得自造**：若原文多个主题共用一个章节编号（无官方子编号），直译/导读的标题必须与原文一致（如 `### 2.16 Special Built-In Utilities`），各主题细分用**无编号粗体**（如 `**break**`、`**case**`）。（注：POSIX 特殊内置有官方锚点编号 tag_19_16…tag_19_30，故可用 `### 2.16 break`；无此依据时禁止自造。）

---

## 7. 验证清单（每篇必跑）

1. **pandoc 渲染**：`pandoc <file>.md -o /tmp/v.html`（translate/AI-guide）无 WARNING、EXIT=0；有图片时 `--embed-resources --resource-path=.`。中文裸 `$` 会报 `Could not convert TeX math`——先修转义再谈其他。
2. **结构数量**：`<table>`/`<blockquote>`/`<pre>`/`<h2-5>` 数量与源对应（脚本对比）。
3. **表格逐格**：每个表格的行数、列数、colspan 与官方一致（脚本解析 `<tr>`/`<td>` 对比）。
4. **残留检查**：
   - 无 smart 泄漏弯引号（ASCII 引号被 pandoc smart 转成 `'` `"` `—` 不该出现）；**AI-guide 例外**——导读使用中文引号（“”）是正常书写，不受此限；
   - 无 `\$` 残留、无 `data-math`/math 标签；
   - 无 `&lt;code&gt;` 等字面标签文本；
   - 无 TODO/占位残留。
5. **标题一致性**：直译/AI-guide 的标题编号与英文原文一一对应（脚本提取对比；官方有而译文无 = 漏节，译文有而官方无 = 自造编号）。
6. **信息完整性**：对照官方章节标题清单，确认无漏节；对照官方表格，确认无漏表。
7. **DOM 校验（pandoc 的盲区）**：pandoc 对 raw HTML 是直通模式，标签错误不报 WARNING。用 Python `html.parser`/`BeautifulSoup` 解析生成的 translate/AI-guide 的 `.html`，确保 raw HTML 标签闭合无误（`<table>`/`<td>`/`<a>` 等配对完整），**并拦截单元格内误用 markdown 语法**（`**`、`_`、`#`）——需要加粗/斜体/换行一律用标准 HTML 标签（`<b>`、`<i>`、`<br />`）。

---

## 8. markdown 场景常见陷阱速查

1. 不要用 pandoc 把 HTML 转 markdown（丢 `<dl>` 结构、丢图片）——除非用户明确要求，否则原文直接存 HTML（走 html_spec）。
2. 转义时 `<`/`>` 必须重新编码为 `&lt;`/`&gt;`，否则直接丢失。
3. 表格必须 raw HTML 逐格一致；markdown 表格做不到 colspan/rowspan/无表头。
4. 中文文本里的裸 `$` 必须处理（成对触发 TeX math 报错）；代码块内 `\$` 要恢复为 `$`。
5. inline（生成标签）与 esc（转义文本）的顺序：先转义文本、标签用占位符保护，否则标签变字面文本。
6. 定义列表 `:   ` 后的 fenced 代码块开始行带冒号前缀，fenced 检测要 `lstrip(': \t')`。
7. AI-guide 标题与原文一一对应，禁止双重编号；细分用无编号粗体。
8. 官方无表头表格 → raw HTML；官方紧凑表（1 行多列）→ 照抄官方布局，不拆多行。
9. 表格列数与官方一致（漏列 → 渲染错位、最后一列全错），用 python 校验每行 `|` 数或 `<td>` 数。
