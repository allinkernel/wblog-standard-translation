# 标准文档"三兄弟"翻译工作规范（spec.md）

> **版本**：v1.0（2026-08-26，基于 POSIX.1-2024 项目实战经验总结）
> **适用范围**：任意 Agent 接入任意大语言模型，翻译任何标准/规范类文档（POSIX、RFC、ISO 等）。
> **本文档同时是给人类读者的说明书和给 Agent 的操作手册。** 用户修改本文件后，Agent 应以修改后的版本为准。

---

## 0. 总览

### 0.1 目标

把一篇标准文档（或一个标准卷的多篇章节）翻译为**"三兄弟"**形式的本地文档树：

| 兄弟 | 目录后缀 | 内容 | 性质 |
| --- | --- | --- | --- |
| 老大：英文原文 | （无后缀） | 官方 HTML **原样**保存 | 权威底本 |
| 老二：中文直译 | `-translate` | 逐字逐句的中文翻译，**结构与原文一一镜像** | 对照阅读 |
| 老三：AI 导读 | `-AI-guide` | 通读全文后的结构化中文讲解 | 帮助理解 |

三兄弟的**章节标题完全一致**（同编号），便于中英对照。

### 0.2 质量优先级（用户明确定义）

```
原文内容正确 > 原文排版正确 > 译文内容正确 > 译文排版正确 > AI-guide
```

任何一步都不能为了下一步牺牲上一步。**"逐字逐句"原则**：直译与原文"分字不差"——每个句子、每个段落、每个列表项、每个表格单元格都必须对应，包括表格的**格式**（网格线、列数、合并单元格）也要与官方一致。

### 0.3 触发方式（用户只做一件事）

用户输入：

```
请帮我翻译 <link1> <link2> <link3> 并生成规范三兄弟
```

Agent 解析链接 → 按本规范**递归**完成全部工作 → 报告结果。用户不需要中途干预。

### 0.4 递归原则（核心架构）

```
单篇（1 个链接）：
    Agent 自己完成全部工作（下载 → 直译 → 导读 → 验证），不需要子 Agent。

多篇（N 个链接，如同一标准的多个章节）：
    Agent 作为"父"，递归派生 N 个子 Agent，每个子 Agent 按本规范处理一篇；
    子 Agent 内遇到大章节，可再递归拆分翻译为多个孙 Agent；
    全部完成后，父 Agent 汇总（合并子 Agent 记忆、全局视角导读、统一验证、收尾）。
```

每一层的 Agent 都执行同一份规范——这就是"递归"。子 Agent 继承父的规范上下文，父 Agent 保留全局视角。

---

## 1. 输入解析与任务规划

### 1.1 解析用户输入

- 提取所有 URL（`https://...`），区分：
  - **章节页**（如 `.../V3_chap01.html`）：一整章 → 一篇三兄弟
  - **单页**（如 `.../awk.html`）：一个工具/子文档 → 一篇三兄弟
  - **目录页**（如 `.../toc.html`、`contents.html`）：解析出全部子链接 → 生成大纲 + 多篇递归
- 若无链接但用户给了路径，按路径处理。

### 1.2 确定目录名

- 目录名 = 章节/工具的语义名（如 `2.shell`、`6.awk`），**不区分三兄弟**——三兄弟在子目录名上区分（`xxx`、`xxx-translate`、`xxx-AI-guide`）。
- 有官方编号的用编号前缀（如 `2.shell`、`6.awk`、`109.sed`）。
- 三兄弟目录必须**同级并列**，放在共同的父目录下。

### 1.3 规划递归

- 链接数 = 1 → 单篇自完成。
- 链接数 > 1 → 派生 N 个子 Agent（每个一篇），父 Agent 等待全部完成后收尾。
- 单篇内容超长（如整章 3000+ 行）→ 可再拆分为多个翻译子 Agent（按小节切分），各自输出独立片段文件，完成后按顺序合并。

---

## 2. 三兄弟之一：英文原文（老大）

### 2.1 目标

**官方 HTML 原样保存**，保留网页原样式、原内容。**禁止**用 pandoc 把 HTML 转成 markdown（markdown 自身不足会导致排版异常、丢表格结构）。

### 2.2 具体步骤

1. **下载**：`curl -s -o <dir>/index.html "<官方 URL>"`，**原样保存**（含 `<head>`、CSS 引用、导航、版权块全部保留）。
2. **配套资源**：
   - 官方 CSS：下载到同目录（如 `style.css`），保持 `<link href="style.css">` 引用有效。
   - 官方 JS（如有，如 `darkmode.js`）：下载到同目录。
   - 正文引用的图片：官方路径通常是 `../images/xxx.gif` → 下载到 `.pic/xxx.gif`，并把 HTML 中 `src="../images/xxx.gif"` 改写为 `src=".pic/xxx.gif"`（注意跨行的 `src=\n"..."` 也要匹配）。
3. **验证**：
   - `wc -c` 与官方页面大小接近（不应缩水严重）。
   - `grep -c '<table'`、`<blockquote`、`<pre` 等关键结构数量保留。
   - 无 `WARNING: Could not fetch resource`（图片全部本地化）。

### 2.3 备注

- 导航链接（Previous/Home/Next）指向站内其他页，本地 404 **无害**，保留原样。
- 官方页面自身的遮蔽 `[redacted]` 保留（原文就这样）。
- 旧产物（如 HTML 镜像版 `index.md`）是否删除由用户决定，Agent 不擅自删。

---

## 3. 三兄弟之二：中文直译（老二）

### 3.1 总原则：语义零丢失 + 中文自然语序 + 文档结构镜像

"逐字逐句"不是"逐词对位"。翻译必须满足**三个独立层次**，缺一不可：

| 层次 | 要求 | 说明 |
| --- | --- | --- |
| 语义 | **零丢失、零添加** | 每个成分（主语/动作/对象/条件/结果）都要译出，不得省略、不得概括、不得自行补充 |
| 语序 | **中文自然语序** | 拆句、调序、用"如果…则…""当…时…"等中文句式；**不照搬英文从句结构**。英文定语从句前置直译会造成超长前置定语，是阅读障碍的根源 |
| 文档结构 | **镜像原文** | 段对段、条对条、层对层：章节编号（1. 2. 3.）、缩进层级、顶格标记（如 `$`）、`<dl>`/`<ol>`/`<ul>`/`<blockquote>`/`<pre>` 全部一一对应；表格逐格一致 |

- **排版**：中文排版不需要复制原文的行宽/换行，但结构层级必须保留。
- **对照可读性**：译文是给人对照原文读的——句子要一眼能懂，而不是要对着原文猜意思。

### 3.2 翻译"一行文本"的操作步骤（标准动作）

对原文的每一行/每一句，Agent 依次执行：

1. **读原文**：完整读该句，识别句子骨架（主语 + 情态动词 + 谓语 + 宾语/状语）。
2. **识别情态**：
   - `shall` → **应**（规范要求，必须）
   - `shall not` → **不应**
   - `may` → **可以**（允许）
   - `should` → **宜**（建议）
   - `is/are`（陈述） → 直陈（是/为）
   - `can` → **能/可以**（能力）
3. **查术语表**（见附录 A）：特殊术语必须用固定译法，不得随意变。
4. **保留标记**：
   - 交叉引用链接 → `[中文标题](#原文锚点)` 或 `[中文标题](../path)`，锚点 id 与原文一致。
   - 行内代码/记号（`$var`、`<newline>`、`WORD` 等）→ 代码标记包裹，**原样保留不译**。
   - 特殊字符 → 实体转义（见 3.4）。
   - 官方遮蔽 `[redacted]` → 按浏览器实际显示直译 + `[译者注：原文此处交叉引用被官方遮蔽，完整语义应为...]`。
5. **输出译文**：保持原句的列表编号、缩进、层级标记。
6. **自查（关键）**：回读译文，检查——
   - 语义成分是否齐全（对照原文逐成分核对：无漏、无添）；
   - 是否出现**超过一个逗号的长前置定语**（如"被引用字符串中同时位于...之间的输入字符"），有则**拆句重排**；
   - 是否出现"从句套从句"（如"而应定义在单词扩展时其输出替换...的命令"），有则拆成独立分句（"它们构成命令；进行单词扩展时，该命令的输出将替换..."）；
   - 情态词、术语、编号、链接是否正确。

### 3.3 结构镜像的具体规则

| 原文结构 | 译文结构（markdown） |
| --- | --- |
| `<h2>/<h3>/<h4>/<h5>` 标题 | `##` / `###` / `####` / `#####`，标题文本 `中文 (English)` |
| `<ol>` 有序列表 | `1.` `2.` `3.`，**编号必须与原文一致** |
| `<ul>` 无序列表 | `-` |
| `<dl compact>` 定义列表 | pandoc 定义列表：Term 顶格 + 空行 + `:   ` 缩进内容（同一 dd 多段用 4 空格续行） |
| `<blockquote>` | `> ` |
| `<pre>` 代码块 | fenced 代码块 ```` ``` ````（shell/程序代码**原样保留不译**） |
| `<table>` | **raw HTML 表格**（见 3.5），不用 markdown 表格 |
| `[Option Start]/[Option End]` 图标 | `[译者注：原文此处有可折叠 UI 标记（UP 图标与 [Option Start] 图片）...]` |

### 3.4 实体转义（防 pandoc 误解析）

在 markdown 文本节点中，以下字符需转义为 HTML 实体，防止 pandoc 的 smart 转换和 markdown 解析破坏内容：

```
&   →  &amp;      <   →  &lt;       >   →  &gt;
"   →  &#34;      '   →  &#39;      $   →  &#36;
`   →  &#96;      *   →  &#42;      _   →  &#95;
[   →  &#91;      ]   →  &#93;      \   →  &#92;
--- →  &#45;&#45;&#45;   -- →  &#45;&#45;   ... →  &#46;&#46;&#46;
```

- 效果：`&#36;` 显示 `$` 不会触发 TeX math；`&#45;&#45;&#45;` 保持 `---` 字面不会被 smart 转成 em-dash；`&#34;` 显示 `"` 不会变弯引号。
- **代码块内不需要转义**（pandoc 不解析代码块内容）；若代码块内误留 `\$`，恢复为 `$`。
- 中文文本里的裸 `$` 必须转义（`$1、$2 和 $3` 会被 pandoc 当 TeX math 报错）。

### 3.5 表格：raw HTML，逐格一致（最重要）

**markdown 表格是缺陷**（无 colspan/rowspan、列宽不可控、单元格内多行受限），无法复刻官方表格。**所有表格必须用官方同款 raw HTML**，pandoc 对 markdown 中的 raw HTML 表格直通保留。

步骤：

1. 从官方 `index.html` 提取对应 `<table>...</table>` 原文。
2. **保留官方结构**：`border`、`cellpadding`、`colspan`、`rowspan`、`align`、单元格内 `<br>` 多行、嵌套 `<p>` 全部保留。
3. 替换单元格文本为中文（代码/符号/函数名原样）。
4. 表格内文本**实体转义**（防 pandoc 对 raw HTML 内部做 markdown 解析），但**标签本身不转义**。
5. 单元格内 markdown 行内语法转 HTML：`` `x` `` → `<code>x</code>`、`*x*` → `<i>x</i>`、`**x**` → `<b>x</b>`、`[t](u)` → `<a href="u">t</a>`。
6. 官方"1 行多列、每列 `<br>` 多项"的紧凑表（如保留字表、awk 关键字表）→ 照抄官方布局，**不要**拆成多行 markdown 表。
7. 官方无表头的表格（如进程属性 2 列）→ raw HTML 无表头实现（markdown 表格必须有表头行，做不到）。

**致命顺序 bug（务必避免）**：先 `inline_md`（生成 `<code>` 标签）再 `esc_html` 会把标签转义成字面 `&lt;code&gt;` 文本。正确顺序：**先 esc_html 文本，再让行内转换匹配实体形式**，或用占位符保护标签后 esc_html 再还原。

验证：pandoc 渲染后检查 `&lt;code&gt;`/`&lt;i&gt;` 等字面标签残留为 0；表格行数、列数、colspan 与官方逐一比对。

### 3.6 官方遮蔽与异常

- `[redacted]`：官方自身遮蔽的交叉引用 → 按浏览器实际显示直译 + 译者注说明完整语义。
- `class="tent"`（暂定内容）：正常直译，不特殊标注（与样板一致）。
- 原文 HTML 标签残缺（未闭合 `<a>`、`<tt>` 等）：按浏览器实际显示直译。

### 3.7 样板小节

- 工具页的 STDIN/INPUT FILES/ENVIRONMENT VARIABLES/ASYNCHRONOUS EVENTS/STDOUT/STDERR/OUTPUT FILES/EXTENDED DESCRIPTION 八个样板小节合并为一个标题 + 一行汇总（如 `> 标准输入：不使用。输入文件：无。...`），避免样板逐字重复。
- 信息性内容标记：`*以下各节是提供信息的（informative）。*` / `*提供信息文本结束。*`。

---

## 4. 三兄弟之三：AI 导读（老三）

### 4.1 目标与定位

导读**不是**翻译，而是"帮读者理解"的结构化讲解：每一节**为什么存在、核心机制是什么、关键概念怎么用、常见坑在哪**。允许加入自己的理解与示例，**但不得编造规范没有的事实**。

### 4.2 标题铁律（用户明确要求）

- 可以有自己的 **0. 章节**（如 `## 0. 导读总览`、`### 0.1 ...`）——0 章节不参与原文编号。
- **从 1. 开始的标题必须与原文编号一一对应**：`## 1. Introduction`、`### 1.1 ...`、`#### 1.1.1 ...`、`##### 1.1.1.1 ...`，格式 `中文 (English)`，与直译一致。
- **禁止**双重编号（如 `### 1.2 1.1.1 System Interfaces`）和"导读第 X 节"式标题（如 `### 1.4 第 1.2 节：...导读`）。
- 导读自己的细分（"为什么存在"/"核心机制"/"速查表"等）用**无编号粗体** `**...**`，不参与编号体系。
- 工具速查（如 2.16–2.30）每个工具独立标题 `### 2.16 break`，顺序与原文一致，**不得合并多个工具到一个标题**。

### 4.3 对"一行文本/一个规则"做导读的操作步骤

1. **定位**：这句话在整章的机制链条中处于哪一环（读取/解析/扩展/执行/错误处理…）。
2. **语义提炼**：用一句话说出它"真正在说什么"（去掉 shall 的规范性措辞，抓住行为本质）。
3. **机制关联**：它依赖哪些前面的规则？被哪些后面的规则依赖？与哪些规则形成对照（如 `"$@"` vs `"$*"`）？
4. **易错点**：实际使用中这句规则最容易被误解/违反的地方。
5. **示例**（可选）：给出符合/违反该规则的微型例子，帮助直觉理解。
6. **输出格式**：放在对应标题下，用列表/表格/代码块组织，语言**自然流畅**（不生涩、不堆砌术语）。

### 4.4 语言要求

- 中文写作，**流畅可读**，像一位熟悉规范的人在讲解，而不是翻译腔。
- 术语仍按术语表（保持与直译一致，便于对照）。
- 每节末尾可加 `**延伸阅读**`：指向规范中值得精读的相关小节/交叉引用。

### 4.5 全局视角

- 导读必须在**通读全文**之后写——如果翻译是子 Agent 并行完成的，导读必须由**合并了全部子 Agent 记忆的父 Agent**（或重新通读全文的 Agent）在全局视角下生成，不能只看局部。
- 章节之间的交叉引用、前后矛盾的解释、全局性概念（如"执行环境""扩展顺序"）要在导读中串联起来。

---

## 5. 并行、递归与合并

### 5.1 单篇：Agent 自完成

一篇三兄弟（下载 → 直译 → 导读 → 验证）全部由当前 Agent 自己完成。不需要子 Agent。

### 5.2 多篇：递归派生子 Agent

```
父 Agent
 ├─ 规划：N 个链接 → N 个子 Agent（或按工作量分组）
 ├─ 共享规范：把 spec.md（本文件）+ 术语表 + 样板 + 验证清单 下发给每个子 Agent
 ├─ 启动：每个子 Agent 一个独立工作区/独立输出文件（避免写冲突）
 ├─ 等待：全部完成后逐个验收（不信子 Agent 的 self-report，必须自己复核）
 └─ 收尾：全局大纲、统一验证、skill 更新、报告
```

### 5.3 大章节内部：翻译拆分 + 验收

单篇内容超长时（如整章 3000+ 行），子 Agent 可再递归：

```
子 Agent（管理型，不亲自翻译）
 ├─ 拆分为 M 个小节区间 → M 个"翻译孙 Agent"（并行，各写独立 .parts/ 片段文件）
 ├─ 全部完成后 → 1 个"验收孙 Agent"：
 │     ├─ 格式验收：脚本化检查（pandoc 无警告、结构数量、残留实体、表格行数列数）
 │     └─ 语义验收：逐节对照原文，检查漏译/错译/术语一致性，输出"问题清单"
 ├─ 问题清单 → 交回对应翻译孙 Agent 修复 → 复验（闭环）
 └─ 合并片段 → 产出直译 index.md
```

### 5.4 合并子 Agent 记忆

- 每个子 Agent 完成后回报：**产物路径、验收结果、它理解的该部分核心机制/术语/异常**（结构化总结）。
- 父 Agent 汇总这些"记忆"，**在全局视角下**生成/审核 AI 导读、更新术语表、发现跨章节矛盾。
- 父 Agent 保留**最终验收权**：pandoc 全量渲染 + 抽查 + 表格逐格比对，不依赖子 Agent 自报。

### 5.5 共享检出注意事项

- 多个子 Agent 写同一工作区时，bash 可能被协调层禁用 → 各自用独立输出文件（`.parts/`）或 worktree 隔离；需要 bash 的任务（下载、command 检测）可由父 Agent 代做。
- 子 Agent 的 prompt 必须自包含：任务范围（锚点区间）+ 必读规范 + 样板位置 + 输出文件 + 自查要求 + 步数预算（大任务给足，如 300-600 步）。

---

## 6. 验证清单（每篇必跑）

1. **pandoc 渲染**：`pandoc <file>.md -o /tmp/v.html`（translate/AI-guide）无 WARNING、EXIT=0；英文原文 `index.html` 无 `Could not fetch resource`。
2. **结构数量**：`<table>`/`<blockquote>`/`<pre>`/`<h2-5>` 数量与源对应（可脚本对比）。
3. **表格逐格**：每个表格的行数、列数、colspan 与官方一致（脚本解析 `<tr>`/`<td>` 对比）。
4. **残留检查**：
   - 无弯引号（smart 泄漏：`'` `"` `—` 不该出现）
   - 无 `\$` 残留、无 `data-math`/math 标签
   - 无 `&lt;code&gt;` 等字面标签文本
   - 无 TODO/占位残留
5. **标题一致性**：直译/AI-guide 的标题编号与英文原文一一对应（脚本提取对比）。
6. **信息完整性**：对照官方章节标题清单，确认无漏节；对照官方表格，确认无漏表。

---

## 7. 收尾

1. **大纲**：更新父目录 `index.md`（章节清单、三兄弟链接指向 index.html）。
2. **知识沉淀**：把本次学到的新经验更新到 skill/知识库（新坑、新术语、新结构类型），供下次递归使用。
3. **git**：不执行任何 git 写操作（add/commit/push/pull），只报告状态；提交由用户完成。
4. **报告**：产物清单（每个文件的路径+行数+大小）、验证结果、遗留问题、未覆盖部分。

---

## 8. 一次完整执行示例（POSIX 单章）

```
用户：请帮我翻译 https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html 并生成规范三兄弟

Agent：
 1. 解析链接 → 单篇 → 自完成
 2. 建目录：2.shell/、2.shell-translate/、2.shell-AI-guide/
 3. 下载 index.html 原样 + style.css + darkmode.js + .pic/ 图片 + 改写 src
 4. 直译：逐节逐句翻译（术语表+结构镜像+raw HTML 表格），输出 2.shell-translate/index.md
 5. 导读：通读全文 → 2.shell-AI-guide/index.md（0.总览 + 与原文同编号标题 + 无编号细分）
 6. 验证：pandoc + 表格逐格 + 残留检查 + 标题一致性
 7. 生成 index.html 成品（pandoc 博客同款命令）
 8. 更新大纲 + skill + 报告
```

---

## 附录 A：POSIX 术语表（通用）

| 英文 | 中文 | 备注 |
| --- | --- | --- |
| shall / shall not | 应 / 不应 | 规范要求 |
| may | 可以 | 允许 |
| should | 宜 | 建议 |
| unspecified | 未指定的 | 符合标准但行为不由标准保证 |
| undefined | 未定义的 | 违反约束的后果，标准不负责 |
| implementation-defined | 实现定义的 | 实现必须文档化 |
| token / tokenize | token / token 化 | 保留英文 |
| dollar-sign | `\<dollar-sign\>`（美元符号） | 字符名 |
| backslash | `\<backslash\>`（反斜杠） | 字符名 |
| newline | `\<newline\>`（换行） | 字符名 |
| compound_list / program | `*compound_list*` / `*program*` | 文法非终结符保留英文斜体 |
| exit status | 退出状态 | |
| wait status | 等待状态 | |
| current shell execution environment | 当前 shell 执行环境 | |
| subshell environment | 子 shell 环境 | |
| special built-in | 特殊内置 | |
| declaration utility | 声明实用程序 | |
| assignment context | 赋值上下文 | |
| positional parameter | 位置参数 | |
| special parameter | 特殊参数 | |
| pattern matching | 模式匹配 | |
| job control | 作业控制 | |

> 各标准领域应建立自己的术语表；新术语在首次遇到时定稿并沉淀到 skill。

## 附录 B：常见陷阱速查

1. 不要用 pandoc 把 HTML 转 markdown（丢 `<dl>` 结构、丢图片）——原文直接存 HTML。
2. 转义时 `<`/`>` 必须重新编码为 `&lt;`/`&gt;`，否则直接丢失（344 个 `&lt;` 全没了的教训）。
3. 表格必须 raw HTML 逐格一致；markdown 表格做不到 colspan。
4. inline（生成标签）与 esc（转义文本）的顺序：先转义文本、标签用占位符保护，否则标签变字面文本。
5. AI-guide 标题与原文一一对应，禁止双重编号；细分用无编号粗体。
6. 子 Agent 的 self-report 不可全信，父 Agent 必须自己复核（pandoc + 抽查）。
7. 共享检出中多个子 Agent 写文件时 bash 可能被禁，用独立文件或 worktree。
8. 代码块内的 `\$` 要恢复为 `$`（代码块内 pandoc 不解析，`\$` 会显示反斜杠）。
