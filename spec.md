# 标准文档"三兄弟"翻译工作规范（spec.md）

> **版本**：v1.9（2026-08-26；历次迭代：v1.0 初版 → v1.2 [redacted] 先验证 → v1.3 md5 双基线 → v1.4 base64 闭环 → v1.5 管道实测定稿 → v1.6 拓扑与主语 → v1.7 token 实测 → v1.8 知识卡片 → v1.9 成本机制 + 缺陷修正）
> **适用范围**：任意 Agent 接入任意大语言模型，翻译任何标准/规范类文档（POSIX、RFC、ISO 等）。
> **本文档同时是给人类读者的说明书和给 Agent 的操作手册。** 用户修改本文件后，Agent 应以修改后的版本为准。

---

## 0. 总览

### 0.1 目标

把一篇标准文档（或一个标准卷的多篇章节）翻译为**"三兄弟"**形式的本地文档树：

| 兄弟 | 目录后缀 | 内容 | 性质 |
| --- | --- | --- | --- |
| 老大：英文原文 | （无后缀） | 官方 HTML **原样**保存 | 权威底本 |
| 老二：中文直译 | `-translate` | 逐字逐句的中文翻译，**结构与原文一一镜像** | 对照阅读 |
| 老三：AI 导读 | `-AI-guide` | 通读全文后的结构化中文讲解 | 帮助理解 |

三兄弟的**章节标题完全一致**（同编号），便于中英对照。

### 0.2 质量优先级（用户明确定义）

```
原文内容正确 > 原文排版正确 > 译文内容正确 > 译文排版正确 > AI-guide
```

任何一步都不能为了下一步牺牲上一步。**"逐字逐句"原则**：直译与原文"分字不差"——每个句子、每个段落、每个列表项、每个表格单元格都必须对应，包括表格的**格式**（网格线、列数、合并单元格）也要与官方一致。

### 0.3 触发方式（用户用自然语言表达意图即可）

**不设固定句式**。任何表达"翻译/转换某标准文档"意图的输入都算触发，例如：

- `请帮我翻译 <link1> <link2> <link3> 并生成规范三兄弟`
- `翻译整个 ISO C11 标准`
- `把 POSIX 的 XBD 卷也做一下`

Agent 需要从自然语言中解析：**翻译对象**（链接 / 标准名 / 卷名）、**范围**（单篇 / 多篇 / 整个标准）、**产物要求**（默认三兄弟）。

### 0.3.1 计划先行（大任务必须审批，小任务可直行）

**两种情况分开处理：**

| 输入类型 | 行为 |
| --- | --- |
| 已给出明确链接（1 个或几个） | 直接按本规范执行（用户意图明确，无需审批） |
| 只给标准/卷名（如 "ISO C11 标准"）或范围很大（整个标准、整卷） | **先输出翻译计划，等用户批准后再开工** |

**翻译计划必须包含以下信息（供用户判断）：**

1. **链接收集**：Agent 自行检索该标准的官方/权威来源，收集各章节链接（不得只凭记忆列链接，要实际访问验证）。
2. **章节分列**：按章节/子文档列出清单（章节号、标题、链接）。
3. **目录规划**：每个章节的三兄弟将输出到哪个目录（`<父>/<章节>/`、`<父>/<章节>-translate/`、`<父>/<章节>-AI-guide/`）。
4. **工作量预估**：每个章节预计花费的 token 量（按章节规模估算，如源 HTML 字节数 × 系数 + 导读开销）与总预估。
5. **执行顺序建议**（可选）：大章节优先 / 并行拆分建议。

计划输出后**停下来等用户确认**（"继续"/"直接开始"/"调整范围"）。用户批准后才进入第 1 节及之后的执行流程。

> 理由：整标准翻译是小时级到天级的大工程，先让用户看到"要建哪些目录、花多少 token"，避免无脑开工后返工。

### 0.4 递归原则（核心架构）

```
单篇（1 个链接）：
    Agent 自己完成全部工作（下载 → 直译 → 导读 → 验证），不需要子 Agent。

多篇（N 个链接，如同一标准的多个章节）：
    Agent 作为"父"，递归派生 N 个子 Agent，每个子 Agent 按本规范处理一篇；
    子 Agent 内遇到大章节，可再递归拆分翻译为多个孙 Agent；
    全部完成后，父 Agent 汇总（合并子 Agent 记忆、全局视角导读、统一验证、收尾）。
```

每一层的 Agent 都执行同一份规范——这就是"递归"。子 Agent **继承父的规范文档内容**（spec.md/术语表/样板，是静态知识），但**默认 fresh 上下文**（不继承父的全部对话历史与工具状态，见 0.5 成本机制）——父 Agent 保留全局视角。

### 0.5 成本机制须知（先懂计费，再谈优化）

**LLM 交互的底层机制（三层）：**

1. **平台保存状态**：对话历史、工具结果、文件操作记录都保存在平台（如 Codewhale）侧。
2. **每次请求重发全上下文**：平台把"完整累积上下文 + 本次请求"打包发给云端模型。模型本身**无状态**——它每次都是把整段历史重新读一遍，不"记得"上次请求。
3. **前缀缓存**：模型提供商对"未变化的历史前缀"做缓存，命中则跳过重复处理（更快更便宜）。但缓存只在历史前缀不变时生效——上下文一旦追加内容，新部分仍需完整处理。

**计费原理（为什么上下文体积是核心成本）：**

- 每轮成本 ≈（输入 token = 累积上下文 + 本次请求）+（输出 token = 本次回复）；
- 上下文越长，**每一轮**的输入 token 越大，成本随时间累积；
- 所以**控制上下文体积 = 控制总成本**，比任何单次优化都根本。

**省钱策略（映射到本规范的既有机制）：**

| 机制 | 原理 | 对应章节 |
| --- | --- | --- |
| 片段投递 | 上下文只装当前片段，峰值最小化 | 3.6 |
| 知识卡片 | 通读全卷但上下文只装精华索引，不装全文 | 4.5 / 附录 C |
| 子 Agent 独立上下文 | 子 Agent 用 fresh 上下文，不继承父的全部历史（除非确需继承） | 5.x |
| 压缩传输 | 仅"搬运不阅读"场景省 73%（LLM 阅读时是负优化，禁用） | 3.6 |
| 任务边界明确 | 一次只做一件事，避免反复重读大段历史 | 全文 |

**对 Agent 的硬约束：**

- 大文件（>10KB）一律走片段投递，禁止整文件进上下文；
- "通读"用知识卡片提炼，不用全文；
- 派生子 Agent 时优先 fresh 上下文（`fork_context=false`），仅当子 Agent 确需父的规范/记忆时才继承；
- 每轮回复前自查：这次调用是否有必要？是否能合并？——多一次往返就多一轮全上下文成本。

---

## 1. 输入解析与任务规划

### 1.1 解析用户输入

- 输入是**自然语言**，可能包含：链接、标准/卷名、范围描述。解析步骤：
  1. 提取所有 URL（`https://...`），区分：
     - **章节页**（如 `.../V3_chap01.html`）：一整章 → 一篇三兄弟
     - **单页**（如 `.../awk.html`）：一个工具/子文档 → 一篇三兄弟
     - **目录页**（如 `.../toc.html`、`contents.html`）：解析出全部子链接 → 生成大纲 + 多篇递归
  2. 无 URL 但有标准/卷名 → 按 0.3.1 先做链接收集与翻译计划（需用户批准）。
  3. 若无链接但用户给了本地路径，按路径处理。
- **范围判断**：若解析结果为大工程（整标准/整卷/多篇），先走 0.3.1 计划审批；用户明确"直接开始"或只给 1-2 个链接则直行。

### 1.2 确定目录名

- 目录名 = 章节/工具的语义名（如 `2.shell`、`6.awk`），**不区分三兄弟**——三兄弟在子目录名上区分（`xxx`、`xxx-translate`、`xxx-AI-guide`）。
- 有官方编号的用编号前缀（如 `2.shell`、`6.awk`、`109.sed`）。
- 三兄弟目录必须**同级并列**，放在共同的父目录下。

### 1.3 规划递归

- 链接数 = 1 → 单篇自完成。
- 链接数 > 1 → 派生 N 个子 Agent（每个一篇），父 Agent 等待全部完成后收尾。
- 单篇内容超长（如整章 3000+ 行）→ 可再拆分为多个翻译子 Agent（按小节切分），各自输出独立片段文件，完成后按顺序合并。

---

## 2. 三兄弟之一：英文原文（老大）

### 2.1 目标

**官方 HTML 原样保存**，保留网页原样式、原内容。**禁止**用 pandoc 把 HTML 转成 markdown（markdown 自身不足会导致排版异常、丢表格结构）。

### 2.2 具体步骤

1. **下载**：`curl -s -o <dir>/index.html "<官方 URL>"`，**原样保存**（含 `<head>`、CSS 引用、导航、版权块全部保留）。
2. **配套资源**：
   - 官方 CSS：下载到同目录（如 `style.css`），保持 `<link href="style.css">` 引用有效。
   - 官方 JS（如有，如 `darkmode.js`）：下载到同目录。
   - 正文引用的图片：官方路径通常是 `../images/xxx.gif` → 下载到 `.pic/xxx.gif`，并把 HTML 中 `src="../images/xxx.gif"` 改写为 `src=".pic/xxx.gif"`（注意跨行的 `src=\n"..."` 也要匹配）。
3. **记录 md5 基线（必须）**：下载完成后立即
   ```bash
   md5sum index.html > index.html.md5
   ```
   把哈希值存到 `.md5` 文件（或记录在任务笔记中）。这是后续校验"Agent→LLM 传输是否无损"的**唯一权威基线**。
4. **验证**：
   - `wc -c` 与官方页面大小接近（不应缩水严重）。
   - `grep -c '<table'`、`<blockquote`、`<pre` 等关键结构数量保留。
   - 无 `WARNING: Could not fetch resource`（图片全部本地化）。

### 2.3 备注

- 导航链接（Previous/Home/Next）指向站内其他页，本地 404 **无害**，保留原样。
- **读到 `[redacted]` 或任何异常内容时，默认先怀疑是"Agent → LLM 传输/显示层"问题**（工具输出管道可能把链接等模式替换成 `[redacted]`，文件本身是完整的）。必须按 3.6 做 **base64 + md5 双基线闭环**（下载时 `md5sum index.html > index.html.md5` 记录基线 A；读原文**默认走 base64 通道**，收到的 base64 写回再解码算 md5 对比）：A==C 才可信；不一致则分块重取。只有确认文件真实内容确实是 `[redacted]`，才算官方遮蔽。
- 旧产物（如 HTML 镜像版 `index.md`）是否删除由用户决定，Agent 不擅自删。

---

## 3. 三兄弟之二：中文直译（老二）

### 3.1 总原则：语义零丢失 + 中文自然语序 + 文档结构镜像

"逐字逐句"不是"逐词对位"。翻译必须满足**三个独立层次**，缺一不可：

| 层次 | 要求 | 说明 |
| --- | --- | --- |
| 语义 | **零丢失、零添加** | 每个成分（主语/动作/对象/条件/结果）都要译出，不得省略、不得概括、不得自行补充 |
| 语序 | **中文自然语序** | 拆句、调序、用"如果…则…""当…时…"等中文句式；**不照搬英文从句结构**。英文定语从句前置直译会造成超长前置定语，是阅读障碍的根源 |
| 文档结构 | **镜像原文** | 段对段、条对条、层对层：章节编号（1. 2. 3.）、缩进层级、顶格标记（如 `$`）、`<dl>`/`<ol>`/`<ul>`/`<blockquote>`/`<pre>` 全部一一对应；表格逐格一致 |

- **排版**：中文排版不需要复制原文的行宽/换行，但结构层级必须保留。
- **对照可读性**：译文是给人对照原文读的——句子要一眼能懂，而不是要对着原文猜意思。

### 3.2 翻译"一行文本"的操作步骤（标准动作）

对原文的每一行/每一句，Agent 依次执行：

1. **读原文**：完整读该句，识别句子骨架（主语 + 情态动词 + 谓语 + 宾语/状语）。
2. **识别情态**：
   - `shall` → **应**（规范要求，必须）
   - `shall not` → **不应**
   - `may` → **可以**（允许）
   - `should` → **宜**（建议）
   - `is/are`（陈述） → 直陈（是/为）
   - `can` → **能/可以**（能力）
3. **查术语表**（见附录 A）：特殊术语必须用固定译法，不得随意变。
4. **保留标记**：
   - 交叉引用链接 → `[中文标题](#原文锚点)` 或 `[中文标题](../path)`，锚点 id 与原文一致。
   - 行内代码/记号（`$var`、`<newline>`、`WORD` 等）→ 代码标记包裹，**原样保留不译**。
   - 特殊字符 → 实体转义（见 3.4）。
   - 读到 `[redacted]` → **先做 3.6 的 md5 双基线校验**（极可能是 Agent→LLM 传输层替换），确认为官方遮蔽后才加注。
5. **输出译文**：保持原句的列表编号、缩进、层级标记。
6. **自查（关键）**：回读译文，检查——
   - 语义成分是否齐全（对照原文逐成分核对：无漏、无添）；
   - 是否出现**超过一个逗号的长前置定语**（如"被引用字符串中同时位于...之间的输入字符"），有则**拆句重排**；
   - 是否出现"从句套从句"（如"而应定义在单词扩展时其输出替换...的命令"），有则拆成独立分句（"它们构成命令；进行单词扩展时，该命令的输出将替换..."）；
   - 情态词、术语、编号、链接是否正确。

### 3.3 结构镜像的具体规则

| 原文结构 | 译文结构（markdown） |
| --- | --- |
| `<h2>/<h3>/<h4>/<h5>` 标题 | `##` / `###` / `####` / `#####`，标题文本 `中文 (English)` |
| `<ol>` 有序列表 | `1.` `2.` `3.`，**编号必须与原文一致** |
| `<ul>` 无序列表 | `-` |
| `<dl compact>` 定义列表 | pandoc 定义列表：Term 顶格 + 空行 + `:   ` 缩进内容（同一 dd 多段用 4 空格续行） |
| `<blockquote>` | `> ` |
| `<pre>` 代码块 | fenced 代码块 ```` ``` ````（shell/程序代码**原样保留不译**） |
| `<table>` | **raw HTML 表格**（见 3.5），不用 markdown 表格 |
| `[Option Start]/[Option End]` 图标 | `[译者注：原文此处有可折叠 UI 标记（UP 图标与 [Option Start] 图片）...]` |

### 3.4 实体转义（防 pandoc 误解析）

在 markdown 文本节点中，以下字符需转义为 HTML 实体，防止 pandoc 的 smart 转换和 markdown 解析破坏内容：

```
&   →  &amp;      <   →  &lt;       >   →  &gt;
"   →  &#34;      '   →  &#39;      $   →  &#36;
`   →  &#96;      *   →  &#42;      _   →  &#95;
[   →  &#91;      ]   →  &#93;      \   →  &#92;
--- →  &#45;&#45;&#45;   -- →  &#45;&#45;   ... →  &#46;&#46;&#46;
```

- 效果：`&#36;` 显示 `$` 不会触发 TeX math；`&#45;&#45;&#45;` 保持 `---` 字面不会被 smart 转成 em-dash；`&#34;` 显示 `"` 不会变弯引号。
- **代码块内不需要转义**（pandoc 不解析代码块内容）；若代码块内误留 `\$`，恢复为 `$`。
- 中文文本里的裸 `$` 必须转义（`$1、$2 和 $3` 会被 pandoc 当 TeX math 报错）。

### 3.5 表格：raw HTML，逐格一致（最重要）

**markdown 表格是缺陷**（无 colspan/rowspan、列宽不可控、单元格内多行受限），无法复刻官方表格。**所有表格必须用官方同款 raw HTML**，pandoc 对 markdown 中的 raw HTML 表格直通保留。

步骤：

1. 从官方 `index.html` 提取对应 `<table>...</table>` 原文。
2. **保留官方结构**：`border`、`cellpadding`、`colspan`、`rowspan`、`align`、单元格内 `<br>` 多行、嵌套 `<p>` 全部保留。
3. 替换单元格文本为中文（代码/符号/函数名原样）。
4. 表格内文本**实体转义**（防 pandoc 对 raw HTML 内部做 markdown 解析），但**标签本身不转义**。
5. 单元格内 markdown 行内语法转 HTML：**先 esc_html（步骤 4）后**，行内转换要匹配**实体形式**（反引号已转成 `&#96;`、星号已转成 `&#42;`），或用占位符保护标签（见下方"致命顺序 bug"）。
6. 官方"1 行多列、每列 `<br>` 多项"的紧凑表（如保留字表、awk 关键字表）→ 照抄官方布局，**不要**拆成多行 markdown 表。
7. 官方无表头的表格（如进程属性 2 列）→ raw HTML 无表头实现（markdown 表格必须有表头行，做不到）。

**致命顺序 bug（务必避免）**：先 `inline_md`（生成 `<code>` 标签）再 `esc_html` 会把标签转义成字面 `&lt;code&gt;` 文本。正确顺序：**先 esc_html 文本，再让行内转换匹配实体形式**，或用占位符保护标签后 esc_html 再还原。

验证：pandoc 渲染后检查 `&lt;code&gt;`/`&lt;i&gt;` 等字面标签残留为 0；表格行数、列数、colspan 与官方逐一比对。

### 3.6 传输完整性校验（md5 双基线闭环）+ 官方遮蔽处理

**执行环境拓扑（先搞清"谁在哪做什么"，再读流程）：**

```
┌─────────────────────────────────────────────┐
│ 用户 WSL2（真实机器，文件系统的唯一所在地）          │
│   · 所有文件：index.html、.md5、.pic/、译文……    │
│   · 所有命令执行：curl / md5sum / base64 / tar…  │
│   · 执行者：工具（bash/python，运行在 WSL2 内）     │
└───────────────▲─────────────────┬─────────────┘
                │ 工具输出 → 管道    │ LLM 指令 → 工具
                │ （此处可能被污染）  │ （write/bash 调用）
┌───────────────┴─────────────────▼─────────────┐
│ Codewhale 管道（LLM 与 WSL2 之间唯一传输路径）      │
└───────────────▲─────────────────┬─────────────┘
                │ 传输（污染点）     │
┌───────────────┴─────────────────▼─────────────┐
│ LLM（云端模型，不在 WSL2 上，只能通过工具间接操作）    │
└─────────────────────────────────────────────┘
```

**三个关键事实（理解一切的前提）：**

1. **工具 = 跑在用户 WSL2 里的程序**。我（LLM）调用 bash，bash 就在你 WSL2 上执行——读写你的文件、跑你的命令。所以"下载""md5sum""base64"这些动作的**主语是工具、地点是你 WSL2**。
2. **LLM 不在 WSL2 上，也不能直接读写文件**。我"写文件"的唯一方式是：调用 write 工具，由工具在 WSL2 上创建/覆盖文件。我"读文件"的唯一方式是：工具把内容输出，经管道传给我。
3. **管道是唯一传输路径，且可能污染内容**（实测：文本特定组合被替换成 `[redacted]`、二进制变乱码；base64 干净）。所以"内容从 WSL2 到 LLM"和"内容从 LLM 到 WSL2"都经过管道——这就是必须 md5 闭环的原因：**检测污染，而不是防止污染**。

**流程动作逐条带主语（主语=执行者，位置=WSL2/管道/LLM）：**

| # | 动作 | 主语（位置） | 谓语 | 宾语 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 1 | 下载 | 工具（WSL2） | 执行 curl 下载 | 官方 HTML → `index.html` | `curl -o index.html <URL>` |
| 2 | 基线 A | 工具（WSL2） | 执行 md5sum | `index.html` → 哈希 A | `md5sum index.html > index.html.md5` |
| 3 | base64 取内容 | 工具（WSL2） | 执行 base64 编码 | `index.html` → base64 串 | `base64 -w 0 index.html` |
| 4 | 传输 | 管道（Codewhale） | 传输 | base64 串 → LLM 上下文 | 实测对 base64 无污染 |
| 5 | 写回 | LLM（通过 write 工具） | 写入 | 收到的 base64 串 → `WSL2:/tmp/verify.b64` | LLM 不能直接写，必须经工具 |
| 6 | 解码+校验 | 工具（WSL2） | 执行 base64 -d 与 md5sum | `verify.b64` → `verify.md` → 哈希 C | `base64 -d ... && md5sum ...` |
| 7 | 对比 | LLM | 比较 | 哈希 A 与哈希 C | 一致→可信；不一致→分块重取 |
| 8 | 翻译 | LLM（通过 write 工具） | 翻译并写入 | 译文 → `WSL2:xxx-translate/index.md` | 基于 md5 验证过的真实内容 |

> 用户例子的修正：①【下载】主语是"工具"，地点"用户 WSL2"，不是"用户"亲手做；②【LLM 写回】不是"LLM 直接传输回 WSL2"，而是"LLM 调用 write 工具，工具在 WSL2 上创建文件"——LLM 与 WSL2 之间永远隔着一层工具调用。

**token 优化策略（2026-08-26 实测，228KB 真实文件）：**

| 方案 | token | 收益 | 适用场景 |
| --- | --- | --- | --- |
| 直接 base64 全文 | ≈ 76K | 基线 | LLM 需通读可读内容 |
| **压缩 + base64 全文**（gzip 压缩率 26.6%） | ≈ 20K | 搬运省 73.4% | **仅"LLM 不读内容、只搬运验证"**（如完整性测试）；LLM 要阅读时**禁用**（见下） |
| **片段投递**（每次 2KB） | ≈ 667/次 | **上下文峰值降 99%** | 日常翻译/精读 |

- **压缩传输的边界（重要修正）**：压缩数据 LLM **看不懂**（是给工具的解压原料）。若目标是"LLM 通读/理解"，压缩流程 = 20K 输入（搬运）+ 20K 输出（写回）+ 解压后重投 76K = **116K，比直接投递 76K 多花 40K，是负优化**。压缩+base64 只用于"LLM 不读内容、仅搬运+md5 验证"的场景；备份/存储用 gzip/tar.gz 在工具侧完成，不经过 LLM。
- **片段投递规则**：工具按需取片段（`dd`/`sed` 定位），输出"片段 base64 + 片段 md5"，LLM 回写验证后翻译；上下文峰值 = 单个片段而非全文。
- 实测附加发现：管道污染甚至命中算术表达式输出（`(1-x/y)*100` 被替换成 `[redacted]`），md5 闭环是刚需不是可选项。

**管道实测结论（2026-08-26 实验，写死为准）：**

| 通道 | 实测行为 | 结论 |
| --- | --- | --- |
| 纯文本 | 特定内容组合被替换成 `[redacted]`（规则复杂：链接 + 特定前缀/组合触发，可稳定复现） | 不可直接信任 |
| **base64** | **完全干净**（含被遮蔽内容的 base64 串完整输出） | **唯一可靠文本通道** |
| md5 短哈希 | 干净 | 校验锚 |
| **tar.gz 二进制** | **乱码**（字节被文本化，U+FFFD 替换，信息不可逆丢失） | **不能直接传给 LLM** |

**tar.gz 的正确定位**：作为**工具侧内容容器**（原文 + 图片 + `.md5` 打包保管，备份/迁移/多文件管理），**不经过管道**。需要翻译时由工具侧解压、投递片段、片段配 md5。LLM 的接收通道本质是文本，任何二进制必须先文本化——文本化的无损编码就是 base64，所以"传内容给 LLM"一律走 **base64 + md5 闭环**。

**核心思想**：下载后记录文件 md5（基线 A）；内容经管道到达 LLM 后，把**收到的内容**写回临时文件再算 md5（基线 B/C）。**A == 接收内容的 md5 才证明传输无损**；不一致说明管道改动了内容，必须绕开管道取真实内容。

**强制流程：**

1. **下载后**（2.2 已要求）：`md5sum index.html > index.html.md5`，记下基线 A。
2. **读原文一律走 base64 通道**（默认，不先试纯文本）：
   - Agent：按块输出 base64（**默认每块 ≤10KB 原文**，遵守 0.5 硬约束；仅小文件才整传）——`dd if=index.html bs=1 skip=OFFSET count=LEN | base64 -w0`；
   - LLM：把收到的 base64 串原样写回 `/tmp/verify.b64`；
   - 工具侧：`base64 -d /tmp/verify.b64 > /tmp/verify.md && md5sum /tmp/verify.md` 得基线 C；
   - **对比 A 与 C**：
     - 一致 → base64 取回的内容即文件真实内容，作为翻译基线。
     - 不一致 → base64 串在传输中也受损（罕见），分块 base64 逐段校验（每块解码后 md5 对比 A 的对应分块），或重取。
3. **读到 `[redacted]` 的判定**：
   - md5 校验通过（A==C）却仍见 `[redacted]`（极少数）→ 才算官方页面自身遮蔽，保留并加译者注说明完整语义。
   - 否则一律按完整内容翻译，**不得**加译者注。
4. **翻译基线**：始终以"md5 校验通过的内容"为准，**未经 md5 闭环确认的内容一律不采信**。
- `class="tent"`（暂定内容）：正常直译，不特殊标注（与样板一致）。
- 原文 HTML 标签残缺（未闭合 `<a>`、`<tt>` 等）：按浏览器实际显示直译。

### 3.7 样板小节

- 工具页的 STDIN/INPUT FILES/ENVIRONMENT VARIABLES/ASYNCHRONOUS EVENTS/STDOUT/STDERR/OUTPUT FILES/EXTENDED DESCRIPTION 八个样板小节合并为一个标题 + 一行汇总（如 `> 标准输入：不使用。输入文件：无。...`），避免样板逐字重复。
- 信息性内容标记：`*以下各节是提供信息的（informative）。*` / `*提供信息文本结束。*`。

---

## 4. 三兄弟之三：AI 导读（老三）

### 4.1 目标与定位

导读**不是**翻译，而是"帮读者理解"的结构化讲解：每一节**为什么存在、核心机制是什么、关键概念怎么用、常见坑在哪**。允许加入自己的理解与示例，**但不得编造规范没有的事实**。

### 4.2 标题铁律（用户明确要求）

- 可以有自己的 **0. 章节**（如 `## 0. 导读总览`、`### 0.1 ...`）——0 章节不参与原文编号。
- **从 1. 开始的标题必须与原文编号一一对应**：`## 1. Introduction`、`### 1.1 ...`、`#### 1.1.1 ...`、`##### 1.1.1.1 ...`，格式 `中文 (English)`，与直译一致。
- **禁止**双重编号（如 `### 1.2 1.1.1 System Interfaces`）和"导读第 X 节"式标题（如 `### 1.4 第 1.2 节：...导读`）。
- 导读自己的细分（"为什么存在"/"核心机制"/"速查表"等）用**无编号粗体** `**...**`，不参与编号体系。
- 工具速查（如 2.16–2.30）每个工具独立标题 `### 2.16 break`，顺序与原文一致，**不得合并多个工具到一个标题**。

### 4.3 对"一行文本/一个规则"做导读的操作步骤

1. **定位**：这句话在整章的机制链条中处于哪一环（读取/解析/扩展/执行/错误处理…）。
2. **语义提炼**：用一句话说出它"真正在说什么"（去掉 shall 的规范性措辞，抓住行为本质）。
3. **机制关联**：它依赖哪些前面的规则？被哪些后面的规则依赖？与哪些规则形成对照（如 `"$@"` vs `"$*"`）？
4. **易错点**：实际使用中这句规则最容易被误解/违反的地方。
5. **示例**（可选）：给出符合/违反该规则的微型例子，帮助直觉理解。
6. **输出格式**：放在对应标题下，用列表/表格/代码块组织，语言**自然流畅**（不生涩、不堆砌术语）。

### 4.4 语言要求

- 中文写作，**流畅可读**，像一位熟悉规范的人在讲解，而不是翻译腔。
- 术语仍按术语表（保持与直译一致，便于对照）。
- 每节末尾可加 `**延伸阅读**`：指向规范中值得精读的相关小节/交叉引用。

### 4.5 全局视角（知识卡片机制）

**问题**：规范的知识点是跨章节网状引用的（如"执行环境"在 2.9/2.13/2.15 多处出现）。单章导读脱离全卷讲不清楚，但**全卷全文一次传入 LLM 上下文不现实**（实测 XCU 卷全文本 10-20MB，token 达数百万，上下文窗口差两个数量级）。

**解法：索引先行 + 按需投递，不靠全文**。核心载体是**章节知识卡片（Knowledge Card）**：

1. **每章翻译完成后，产出该章的知识卡片**（结构化总结，目标 ≤ 500 token）：
   - 元信息：章节号、标题、官方链接
   - 核心概念：本章定义的关键概念，每个一句话
   - 引入术语：术语 + 一句话定义
   - 交叉引用：本章引用了哪些章节（引用点）；预计被哪些章节引用
   - 关键规则：本章最重要的 3-10 条规则（含编号，便于定位原文）
   - 易错点：常见误解/坑
   - 重要锚点：关键小节的位置标记（供按需投递定位）
2. **父 Agent 收集全部卡片 → 生成 `knowledge-index.md`（全卷地图）**：各章核心概念、术语表汇总、跨章节引用关系图。
3. **写某章导读时**：
   - 读本章译文（片段投递，峰值小）；
   - 查阅 `knowledge-index.md`（几百 token 的全局地图）；
   - 导读中涉及其他章的概念时，**按需投递**该章对应片段（工具定位 + base64 + md5），交叉引用描述基于投递的真实片段，不凭记忆。
4. **导读的导读**：卷级总导读基于各章导读 + knowledge-index 生成，无需重读全文。
5. 知识卡片同时充当"子 Agent 记忆合并"的实体载体（见 5.4），模板见附录 C。

---

## 5. 并行、递归与合并

### 5.1 单篇：Agent 自完成

一篇三兄弟（下载 → 直译 → 导读 → 验证）全部由当前 Agent 自己完成。不需要子 Agent。

### 5.2 多篇：递归派生子 Agent

```
父 Agent
 ├─ 规划：N 个链接 → N 个子 Agent（或按工作量分组）
 ├─ 共享规范：把 spec.md（本文件）+ 术语表 + 样板 + 验证清单 + 知识卡片模板（附录 C） 下发给每个子 Agent
 ├─ 启动：每个子 Agent 一个独立工作区/独立输出文件（避免写冲突）
 ├─ 等待：全部完成后逐个验收（不信子 Agent 的 self-report，必须自己复核）
 └─ 收尾：全局大纲、统一验证、skill 更新、报告
```

### 5.3 大章节内部：翻译拆分 + 验收

单篇内容超长时（如整章 3000+ 行），子 Agent 可再递归：

```
子 Agent（管理型，不亲自翻译）
 ├─ 拆分为 M 个小节区间 → M 个"翻译孙 Agent"（并行，各写独立 .parts/ 片段文件；读原文一律遵循 3.6 的 base64 + md5 闭环）
 ├─ 全部完成后 → 1 个"验收孙 Agent"：
 │     ├─ 格式验收：脚本化检查（pandoc 无警告、结构数量、残留实体、表格行数列数）
 │     └─ 语义验收：逐节对照原文，检查漏译/错译/术语一致性，输出"问题清单"
 ├─ 问题清单 → 交回对应翻译孙 Agent 修复 → 复验（闭环）
 └─ 合并片段 → 产出直译 index.md
```

### 5.4 合并子 Agent 记忆（知识卡片为记忆实体）

- 每个子 Agent 完成后回报三样：**产物路径、验收结果、章节知识卡片**（按附录 C 模板）。
- **知识卡片就是"记忆"的实体**：它把子 Agent 对本章的理解（核心概念/术语/交叉引用/易错点）固化成几百 token 的结构化文本，父 Agent 攒齐全部卡片即得全卷地图（`knowledge-index.md`）。
- 父 Agent 基于 knowledge-index：**全局视角**生成/审核 AI 导读、更新术语表、发现跨章节矛盾（如两章对同一概念的描述冲突）。
- 父 Agent 保留**最终验收权**：pandoc 全量渲染 + 抽查 + 表格逐格比对，不依赖子 Agent 自报。

### 5.5 共享检出注意事项

- 多个子 Agent 写同一工作区时，bash 可能被协调层禁用 → 各自用独立输出文件（`.parts/`）或 worktree 隔离；需要 bash 的任务（下载、command 检测）可由父 Agent 代做。
- 子 Agent 的 prompt 必须自包含：任务范围（锚点区间）+ 必读规范 + 样板位置 + 输出文件 + 自查要求 + 步数预算（大任务给足，如 300-600 步）。

---

## 6. 验证清单（每篇必跑）

1. **pandoc 渲染**：`pandoc <file>.md -o /tmp/v.html`（translate/AI-guide）无 WARNING、EXIT=0；英文原文 `index.html` 无 `Could not fetch resource`。
2. **结构数量**：`<table>`/`<blockquote>`/`<pre>`/`<h2-5>` 数量与源对应（可脚本对比）。
3. **表格逐格**：每个表格的行数、列数、colspan 与官方一致（脚本解析 `<tr>`/`<td>` 对比）。
4. **残留检查**：
   - 直译无 smart 泄漏弯引号（ASCII 引号被 pandoc smart 转成 `'` `"` `—` 不该出现）；**AI-guide 例外**——导读使用中文引号（“”）是正常书写，不受此限
   - 无 `\$` 残留、无 `data-math`/math 标签
   - 无 `&lt;code&gt;` 等字面标签文本
   - 无 TODO/占位残留
5. **标题一致性**：直译/AI-guide 的标题编号与英文原文一一对应（脚本提取对比）。
6. **信息完整性**：对照官方章节标题清单，确认无漏节；对照官方表格，确认无漏表。

---

## 7. 收尾

1. **大纲**：更新父目录 `index.md`（章节清单、三兄弟链接指向 index.html）。
2. **知识沉淀**：把本次学到的新经验更新到 skill/知识库（新坑、新术语、新结构类型），供下次递归使用。
3. **git**：不执行任何 git 写操作（add/commit/push/pull），只报告状态；提交由用户完成。
4. **报告**：产物清单（每个文件的路径+行数+大小）、验证结果、遗留问题、未覆盖部分。

---

## 8. 一次完整执行示例（POSIX 单章）

```
用户：请帮我翻译 https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html 并生成规范三兄弟

Agent：
 1. 解析链接 → 单篇 → 自完成
 2. 建目录：2.shell/、2.shell-translate/、2.shell-AI-guide/
 3. 下载 index.html 原样 + style.css + darkmode.js + .pic/ 图片 + 改写 src
 4. 直译：逐节逐句翻译（术语表+结构镜像+raw HTML 表格），输出 2.shell-translate/index.md
 5. 知识卡片：产出 2.6 等各章卡片（附录 C）→ 汇总 knowledge-index.md
 6. 导读：读本章译文（片段投递）+ 查 knowledge-index + 按需投递被引用章节片段 → 2.shell-AI-guide/index.md
 7. 验证：pandoc + 表格逐格 + 残留检查 + 标题一致性
 8. 生成 index.html 成品（pandoc 博客同款命令）
 9. 更新大纲 + skill + 报告
```

---

## 附录 A：POSIX 术语表（通用）

| 英文 | 中文 | 备注 |
| --- | --- | --- |
| shall / shall not | 应 / 不应 | 规范要求 |
| may | 可以 | 允许 |
| should | 宜 | 建议 |
| unspecified | 未指定的 | 符合标准但行为不由标准保证 |
| undefined | 未定义的 | 违反约束的后果，标准不负责 |
| implementation-defined | 实现定义的 | 实现必须文档化 |
| token / tokenize | token / token 化 | 保留英文 |
| dollar-sign | `\<dollar-sign\>`（美元符号） | 字符名 |
| backslash | `\<backslash\>`（反斜杠） | 字符名 |
| newline | `\<newline\>`（换行） | 字符名 |
| compound_list / program | `*compound_list*` / `*program*` | 文法非终结符保留英文斜体 |
| exit status | 退出状态 | |
| wait status | 等待状态 | |
| current shell execution environment | 当前 shell 执行环境 | |
| subshell environment | 子 shell 环境 | |
| special built-in | 特殊内置 | |
| declaration utility | 声明实用程序 | |
| assignment context | 赋值上下文 | |
| positional parameter | 位置参数 | |
| special parameter | 特殊参数 | |
| pattern matching | 模式匹配 | |
| job control | 作业控制 | |

> 各标准领域应建立自己的术语表；新术语在首次遇到时定稿并沉淀到 skill。

## 附录 B：常见陷阱速查

1. 不要用 pandoc 把 HTML 转 markdown（丢 `<dl>` 结构、丢图片）——原文直接存 HTML。
2. **读到 `[redacted]` 先别信**：极可能是 Agent→LLM 传输/显示层把链接替换成了 `[redacted]`（文件本身完整）。用 **md5 双基线校验**验证（下载时记录 md5 → 读到的内容写回临时文件再算 md5 → 对比；不一致则 base64 取真实内容），见 3.6。
3. 转义时 `<`/`>` 必须重新编码为 `&lt;`/`&gt;`，否则直接丢失（344 个 `&lt;` 全没了的教训）。
4. 表格必须 raw HTML 逐格一致；markdown 表格做不到 colspan。
5. inline（生成标签）与 esc（转义文本）的顺序：先转义文本、标签用占位符保护，否则标签变字面文本。
6. AI-guide 标题与原文一一对应，禁止双重编号；细分用无编号粗体。
7. 子 Agent 的 self-report 不可全信，父 Agent 必须自己复核（pandoc + 抽查）。
8. 共享检出中多个子 Agent 写文件时 bash 可能被禁，用独立文件或 worktree。
9. 代码块内的 `\$` 要恢复为 `$`（代码块内 pandoc 不解析，`\$` 会显示反斜杠）。

---

## 附录 C：章节知识卡片模板（Knowledge Card）

**产出**：每章翻译完成后由该章子 Agent 填写，存为 `<卷>/knowledge-cards/<章节>.card.md`（如 `vol.3.shell/knowledge-cards/2.6-word-expansions.card.md`）。父 Agent 汇总全部卡片生成 `knowledge-index.md`（全卷地图）。

**目标大小**：≤ 500 token/章。宁可精炼，不要堆砌——卡片是索引不是全文。

```markdown
# 知识卡片：<章节号> <章节标题>

## 元信息
- 章节号：<如 2.6>
- 标题：<如 Word Expansions（单词扩展）>
- 原文链接：<官方 URL>
- 译文位置：<路径>

## 核心概念（每项一句话，最多 8 条）
- <概念名>：<一句话定义>（如：字段分割：按 IFS 将未引用的扩展结果切成多个字段）
- ...

## 引入术语（术语 → 一句话定义）
- <术语>：<定义>（如：word expansion：解析后执行前对词的处理全过程）
- ...

## 交叉引用
- 引用了：<章节>（<引用点说明>）……（如：2.9.1.1 简单命令处理顺序引用了本节的七阶段）
- 被引用：<章节>（<何处引用>）……（如：2.14 模式匹配被 2.6.2 的 ${p#pat} 使用）
- 全局性概念：<本概念在哪些章节反复出现>（如："执行环境"出现于 2.9/2.13/2.15）

## 关键规则（3-10 条，带编号便于定位原文）
- <编号> <规则一句话>（如：2.6 七阶段扩展顺序：波浪号→参数→命令替换→算术→字段分割→路径名→引用消除）
- ...

## 易错点
- <常见误解/坑>（如：`"$@"` vs `"$*"`；字段分割只作用于未引用的扩展结果）
- ...

## 重要锚点（供按需投递定位）
- <小节/关键句特征>：<锚点或定位关键词>（如：七阶段顺序表：原文 "The order of word expansion" 附近）
- ...
```

**填写要点**：
- 核心概念/术语是**本章"新增或定义"的**，不是重复常识；
- 交叉引用要具体到"哪节引用了哪节、为什么"——这是全卷地图最有价值的部分；
- 易错点从翻译过程中遇到的难点提炼（译者视角 = 读者视角）；
- 重要锚点给"定位关键词"（原文里独特的短语），供按需投递时 `grep`/`python find` 定位。

**knowledge-index.md 结构**（父 Agent 汇总）：
- 各章卡片索引（章节号 → 卡片路径）
- 全局术语表（跨章合并去重）
- 跨章节引用关系图（哪章引用哪章，网状关系一目了然）
- 全局性概念清单（如"执行环境""扩展顺序""退出状态"——这些概念在导读中要串联讲）

**使用场景**：写导读、跨章一致性检查、卷级总导读，都先查 knowledge-index，再按需投递目标章节片段（base64 + md5），不凭记忆引用。
